### 再让我们看一下helloworld的代码
```eval-cpp
#include<iostream>
using namespace std;
int main()
{
    cout<<"hello,world";
    return 0;
}
```
## 分析

C++ 语言定义了一些头文件，这些头文件包含了程序中必需的或有用的信息。上面这段程序中，通过include语句包含了头文件 <iostream>。

下一行 using namespace std; 告诉编译器使用 std 命名空间。命名空间是 C++ 中一个相对新的概念。std是标准命名空间，c++标准库的命令都在其中

下一行 int main() 是主函数，程序从这里开始执行。

下一行 cout << "Hello,World"; 会在屏幕上显示消息 "Hello,World"。这是输出，我们在下面会学到

下一行 return 0; 终止 main( )函数，并向调用进程返回值 0。一般来说，非零的返回值都意味着某种错误

## C++ 中的分号

在 C++ 中，分号是语句结束符。也就是说，每个语句必须以分号结束。它表明一个语句的结束，也就是一个逻辑实体的结束。

## C++ 的空格

只包含空格的行，被称为空白行，可能带有注释，C++ 编译器会完全忽略它。

在 C++ 中，空格用于描述空白符、制表符、换行符和注释。空格分隔语句的各个部分

# c++的注释

注释可以帮助自己阅读 程序

```eval-cpp
//注释内容

/*
注释内容
其内的注释可以换行
*/
```

# 源文件

以.cpp .cc .cxx为后缀的文件，实现的具体内容

# 头文件

以.h .hpp为后缀，声明函数的场所

# 函数

每个c++程序至少包含一个以上的函数

函数是一个命名的代码块。

执行我们定义好的操作。

# 语句

语句是c++程序的基本单元。

可以把几个语句放在一个花括号中，语句块